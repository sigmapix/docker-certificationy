# docker-certificationy
## RUN
```
  docker run -it sigmapix/certificationy
  or
  docker run -it sigmapix/certificationy --number=10
  or
  docker run -it sigmapix/certificationy --list
```

## Detach/Quit with [Ctrl+P Q]
